using System;
using Xunit;

namespace SecureFlight.Test
{
    public class AirportTests
    {
        [Fact]
        [TestData]
        public void Update_Succeeds(modifiedAirport)
        {
            /*
            var airportUpdated =  airportController.Put(ModifiedAirpoty);
            s
            var aiports = airportController?.getAirports();

            var airportToCompare = aiports.first(x => x.id = airportUpdated.id);

            Assert.Equals(airportToCompare.City, airportUpdated.City);

             */

        }
    }
}
